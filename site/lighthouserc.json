{
  "//": "Lighthouse CI config. Run with `npm run lighthouse` after `npm run build`.",
  "//assertions": "Targets are Lighthouse >= 0.95 across the board (Phase-5 launch gate). Held at 'warn' until the Phase-4 frontend is built out; tighten to 'error' before first public deploy (see LAUNCH_REVIEW.md).",
  "ci": {
    "collect": {
      "staticDistDir": "./dist",
      "numberOfRuns": 1
    },
    "assert": {
      "assertions": {
        "categories:performance": ["warn", { "minScore": 0.95 }],
        "categories:accessibility": ["warn", { "minScore": 0.95 }],
        "categories:best-practices": ["warn", { "minScore": 0.95 }],
        "categories:seo": ["warn", { "minScore": 0.95 }]
      }
    },
    "upload": {
      "target": "temporary-public-storage"
    }
  }
}
