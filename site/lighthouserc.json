{
  "//": "Lighthouse CI config. Run with `npm run lighthouse` after `npm run build`.",
  "//assertions": "Targets are Lighthouse >= 0.95 across the board (Phase-5 launch gate). Held at 'warn' until final launch polish; tighten to 'error' before the first public deploy (see LAUNCH_REVIEW.md).",
  "ci": {
    "collect": {
      "staticDistDir": "./dist",
      "numberOfRuns": 1,
      "settings": {
        "chromeFlags": "--no-sandbox --headless=new"
      }
    },
    "assert": {
      "assertions": {
        "categories:performance": ["warn", { "minScore": 0.95 }],
        "categories:accessibility": ["warn", { "minScore": 0.95 }],
        "categories:best-practices": ["warn", { "minScore": 0.95 }],
        "categories:seo": ["warn", { "minScore": 0.95 }]
      }
    },
    "upload": {
      "target": "filesystem",
      "outputDir": "./.lighthouseci"
    }
  }
}
